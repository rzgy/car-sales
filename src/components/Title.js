const Title = () => {
  const shopName = "Carisale";
  const description = "we are a car dealership but virtual";

  return (
    <div>
      <h1>{shopName}</h1>
      <h5>{description}</h5>
    </div>
  );
};
export default Title;
